name = "1001terminal-worker" # This will be part of your .workers.dev URL
main = "src/index.ts"
compatibility_date = "2024-05-01" # Use a recent date

# This part is essential for Durable Objects
[durable_objects]
bindings = [
  { name = "GEX_HISTORY_DO", class_name = "HeatmapBuilderDO" }
]

# --- THIS IS THE FIX ---
# Change "new_classes" to "new_sqlite_classes" for the free plan
[[migrations]]
tag = "v1"
new_sqlite_classes = [ "HeatmapBuilderDO" ]